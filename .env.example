# このファイルを .env にコピーして値を埋める（cp .env.example .env）
# .env は git 管理外。ここには実際の値を書かないこと。

# Neon の接続文字列（Vercel では pooled 接続文字列（-pooler 付き）を使う）
DATABASE_URL=

# Google OAuth（Google Cloud Console で発行）
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# auth.js のセッション暗号化キー（`npx auth secret` などで生成）
AUTH_SECRET=
